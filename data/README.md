# Data

Place TEC feature CSV files in this directory.

Expected format:

- one row per time step,
- feature columns first,
- target TEC value in the last column.

The real experimental dataset is not included in this public repository. A small synthetic file, `sample_tec.csv`, is provided only for checking that the pipeline can run.
